# PROJECT_STATUS.md — 当前开发状态

> 状态快照：**2026-09-01**（文档审计时）
> 自测：**44 PASS / 0 FAIL**（`BOOTTEST_PASS_P44_F0`，2026-09-01 主角 VEX-07 重做批次后）
>
> ⚠️ 三态必须严格区分。**已完成** = 实际实现且可工作；
> **部分完成** = 代码存在但有明显限制或缺陷；**计划中** = 还没做。
> 禁止把"计划"写成"已完成"。

---

## 一、已完成（实际实现且可工作）

### 核心框架
- [x] 无构建、零外部依赖、`file://` 直开（唯一依赖是本地 `lib/three.min.js`）
- [x] 13 个 IIFE 模块 + `G` 全局命名空间通信
- [x] 固定步长主循环（1/60，累加器，dt 截断 0.1s，最多补 4 帧）
- [x] 顿帧（hitstop）/ 慢动作（timeScale）/ 震屏（trauma²）
- [x] 320p 像素风渲染 + ACES 色调映射
- [x] 完整状态机（title / play / pause / transition / dead / win）

### 玩家
- [x] 圆形碰撞移动（分轴推进）
- [x] 翻滚闪避（0.26s 位移 / 0.24s 无敌 / 0.42s 冷却 + 残影拖尾）
- [x] 血量 / 护甲（12s 自动回复）/ 护盾层数三层受伤判定
- [x] 击退、无敌帧、受伤演出
- [x] **主角「VEX-07 · 深渊行者」定制建模**（2026-09-01 重做）：
      全覆式头盔+发光目镜条 / 胸甲能量核心 / 肩甲肩刺 / 背包天线能量罐 / 橙红披风 / 持枪臂+扶枪副手；
      配色沿用项目主色（深青装甲+暗钢+橙色警示+青色能量），forward=+X 无魔法角度
- [x] **朝向系统**（2026-09-01 修复）：鼠标世界方向 → `face` → `mesh.rotation.y=-face`，
      面部/身体正前方=武器瞄准方向=鼠标世界方向；瞄准射线 isFinite 守卫（FIX-024）；
      回归锁：步骤 39（8 方向收敛/平滑转身/射线 NaN 守卫/辉光贴目镜）
- [x] 角色随身光照（青白随身光 + 蓝色轮廓 rim 光 + 目镜辉光 sprite）

### 武器与战斗
- [x] **16 种武器**（D/C/B/A 四品阶）
- [x] 520 发子弹对象池（玩家/敌人共用，按 `team` 分流）
- [x] 子步进弹道 + 三层碰撞（墙 / 掩体 / 实体）
- [x] 特殊机制：穿透、弹跳、追踪、回旋、冰霜、电弧链、环绕星刃、三连发、爆炸、激光等
- [x] 全武器长按连发
- [x] 暴击（伤害 ×2.5、弹速 ×1.12）
- [x] 武器切换：Q 循环 / 滚轮双向循环 / 数字键直选槽位（2026-09-01 修复 BUG-003）
- [x] 三连发 burst 队列（切枪自动清队；2026-09-01 修复 BUG-002）

### 敌人
- [x] **12 种敌人** + 精英变体（hp×2.2 / r×1.2 / spd×1.15）
- [x] 每类型独立 AI 状态机
- [x] 盾卫格挡/破防（累计 5 次 → 2.5s 踉跄）
- [x] 出生保护 0.45s、击退、减速、受击闪白、接触伤害
- [x] 死亡掉落（金钱 / 钥匙 / 红心）、史莱姆分裂、甲虫自爆
- [x] 反软锁四道防线（含 0.8s 位置自愈）

### Boss
- [x] Boss「铁颚」HP 900、三阶段（60% / 25% 阈值）
- [x] 10 种攻击状态（加特林 / 扇形 / 冲锋 / 螺旋 / 召唤 / 跳砸 / 弹幕墙 / 出场 / 阶段切换 / 死亡）
- [x] 血条 UI、皇冠击飞演出、慢动作死亡演出
- [x] 可被玩家正常击杀（真实子弹/爆炸链路；2026-09-01 修复 BUG-001，回归锁：步骤 35）

### 地牢
- [x] 种子化随机生成（xorshift32）
- [x] 房间类型：start / combat / shop / treasure / exit / secret / npc / boss / shrine / gamble
- [x] 门即走廊（2×2 开口）、锁定/解锁、隐藏门（裂纹墙 3 发打开）
- [x] Boss 房四级兜底选址（含原子回滚 + BFS 可达性校验）
- [x] 房间生命周期：进入 → 封锁 → 波次 → 清剿 → 开门 → 奖励
- [x] 楼层切换（第 1 层 ⇄ 第 2 层）

### 系统
- [x] 18 种被动道具 + 4 种主动技能
- [x] 商店（货架、名牌、价格牌、购买、满血买红心改扩容）
- [x] 宝箱（brown/green/red 三档）、祭坛、赌桌、NPC
- [x] 掩体系统（桌/桶/罐/柱），翻桌机制（只挡敌方子弹）
- [x] 陷阱（尖刺 / 毒沼，仅第 2 层）
- [x] 交互系统（最近距离选取 + 输入缓冲）
- [x] HUD（心 / 属性 / 武器 / 弹药 / 层名 / 小地图 / 大地图 / Boss 血条）
- [x] 全程序化音频（约 40 个音效 + 4 首 BGM，无任何音频文件）
- [x] VFX 对象池（粒子 / 光源 / 冲击环 / 伤害数字 / 闪电 / 金币星芒）
- [x] 44 步自测套件

---

## 二、部分完成（代码存在，但有明显限制或缺陷）

| 项 | 现状 | 限制 |
|---|---|---|
| 楼层数 | 第 1、2 层完整 | 楼层号硬编码为 2，`B.themes` 只有 1/2。无第三层 |
| 种子可复现性 | 地形与掉落可复现 | 战斗过程用 `Math.random()`，**不可复现**（BUG-022） |
| 幽灵态（残影斗篷） | 接触伤害免伤生效 | 弹幕**会消失但不造成伤害**，未实现"穿过"（BUG-013） |
| 环绕星刃 | 基础功能正常 | 不吃任何被动加成（BUG-010） |
| 数据表驱动 | 武器/敌人数据表齐全 | 部分字段是死字段（`splash` / `chain` / `chainFade`），改了不生效 |
| 道具受击反馈 | 有 `flashT` 字段 | **无消费方，闪光不生效**（BUG-025） |
| 碰撞扫掠 | 分轴 + 前缘单列采样 | **不做扫掠**，高速位移有穿墙风险（BUG-004） |
| 文档体系 | 2026-09-01 建立 | 尚未经过多轮改动的同步检验 |

---

## 三、已知 Bug（未修复）

**P0 已清零（2026-09-01 修复 BUG-001/002/003/005/006，见 BUG_HISTORY.md FIX-019~023）。**
当前剩余：**2 个 P1（均为潜在/低频，已评估暂不修）、19 个 P2、一批 P3 死代码/不一致**。
完整清单见 `KNOWN_ISSUES.md`：

| ID | 级别 | 简述 | 状态 |
|---|---|---|---|
| BUG-004 | P1 | 移动碰撞不做扫掠，高速位移穿墙风险 | 潜在，触发条件出现前不修 |
| BUG-007 | P1 | 子弹池满静默失败，调用方不检查返回值 | 低频，修法需用户确认 |

---

## 四、当前工作

**无进行中的开发任务。**

最近一次代码改动：2026-09-01 16:20-16:30 **主角 VEX-07 视觉重做 + 朝向系统修复**
（改动 `player.js`/`game.js`/`main.js`，自测扩到 44 步全绿，快照 `bt_vex07.html`；
详见 `DEVELOPMENT_LOG.md` 与 `BUG_HISTORY.md` FIX-024）。
此前 14:10-14:35 为 Bug 修复批次（FIX-019~023）；12:56-14:10 为文档审计与上下文体系建立。

---

## 五、后续方向（推断，未经用户确认）

> 以下是根据项目现状**推断**出的合理方向，**不是用户的需求**。
> 动手前必须先与用户确认。

### 高优先（建议先做）

1. ~~修复 BUG-001（Boss 无敌）~~ ✅ 已完成（2026-09-01，含真实子弹回归锁步骤 35）
2. ~~补齐自测覆盖缺口（Boss 战绕开真实链路）~~ ✅ 已完成（步骤 35 走真实子弹链路；
   其余步骤的链路真实性可在后续迭代中继续复查）

### 中优先

3. **建立版本控制**
   项目无 git，13 个模块 5900 行代码没有任何历史保护。
   强烈建议 `git init` 并做首次提交（**需用户同意**，因为会新增 `.git` 目录）。

3. **清理 P3 死代码**
   约有 20 处死字段/死代码（见 `KNOWN_ISSUES.md`）。
   ⚠️ 但必须逐条确认后再删——本项目有大量"看起来没用其实是设计"的地方。

4. **第三层与第三个主题**
   需同时改 `game.js:242 / :201 / :118`、`build.js:588`、`build.js:15-36` 五处。

### 低优先 / 体验优化

5. ~~修 P1 的操作类 Bug（切枪、滚轮、数字键）~~ ✅ 已完成（FIX-020/021）；剩余 P1 为 BUG-004/007（暂缓）
6. 让战斗过程可复现（把 `Math.random()` 换成种子 RNG）—— 但会改变 RNG 序列，影响现有地图生成
7. 让数据表字段真正生效（`splash` / `chain` / `chainFade`）
8. 环绕星刃支持被动加成

---

## 六、不建议做的事

| 不要做 | 原因 |
|---|---|
| 加"整房超时自动清场" | 会摧毁慢节奏战斗，历史 Bug 复发（步骤 25 会失败） |
| 给 `pgeo()` 缓存几何加 `disposable` 标记 | 第二层所有道具集体消失 |
| 改 `G.hurtEnemy` 的参数个数/顺序 | 盾卫格挡行为静默反转，无报错 |
| 调 `update()` 的更新顺序 | 命中错位、清剿延迟、增援波错乱 |
| 改门的 4-tile 结构 | 门位置/闸门朝向/隐藏墙朝向同时错乱 |
| "修复"文字精灵的 `depthTest:false` | 商店价格牌被遮挡 |
| 把翻桌的 `blocksBullets` 统一化 | 玩家会被自己的掩体卡住输出 |
| 把对象池满时的"静默丢弃"改成抢占 | 会打断正在播放的关键特效 |
| 引入任何 npm 包 / CDN / 构建工具 | 违反项目根本约束，破坏 `file://` 直开 |

---

## 相关文档

- 完整缺陷清单 → `KNOWN_ISSUES.md`
- 已修复的历史 Bug → `BUG_HISTORY.md`
- 高危区与禁止的重构 → `HIGH_RISK_AREAS.md`
- 开发时间线 → `DEVELOPMENT_LOG.md`
